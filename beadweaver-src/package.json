{
  "name": "beadweaver",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "main": "src/index.jsx",
  "devDependencies": {
    "@react-three/drei": "^9.114.0",
    "r3f-pack": "^3.0.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "three": "^0.168.0"
  },
  "scripts": {
    "start": "r3f-pack start",
    "build": "r3f-pack build && npm run move-build",
    "move-build": "node -e \"const fs = require('fs'); const path = require('path'); const src = './build'; const dest = '../beadweaver'; if (fs.existsSync(dest)) { fs.rmSync(dest, { recursive: true, force: true }); } fs.renameSync(src, dest); console.log('Build moved to sibling directory: ../build');\""
  },
  "dependencies": {
    "@react-three/csg": "^3.3.0"
  }
}
